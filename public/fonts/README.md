# Schriften

Hier liegen die lizenzierten Web-Font-Dateien (.woff2) der Markenschrift.
Einbindung in `app/[locale]/layout.tsx` über `next/font/local` – nie über Google-Fonts-URLs.
Lizenz für Web-Einbettung muss vorliegen (Pascal). Bis dahin läuft die Site mit System-Schrift.
