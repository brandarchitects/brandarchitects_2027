# Design

Gestaltung wird im separaten Claude-Projekt «BrandArchitects_Design» exploriert (Briefing: `design-projekt-briefing.md`).
Übergabe in den Code ausschliesslich über `design-spec-vN.md` in diesem Ordner; die jeweils gültige Fassung wird nach `docs/design-spec.md` kopiert und von Claude Code umgesetzt (Branch `design/vN`).

Stand: keine Spec vorhanden – Site läuft mit Platzhalter-Tokens (`app/globals.css`).
