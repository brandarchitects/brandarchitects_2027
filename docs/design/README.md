# Design

Gestaltung wird im separaten Claude-Projekt «BrandArchitects_Design» exploriert (Briefing: `design-projekt-briefing.md`).
Übergabe in den Code ausschliesslich über `design-spec-vN.md` in diesem Ordner; die jeweils gültige Fassung wird nach `docs/design-spec.md` kopiert und von Claude Code umgesetzt (Branch `design/vN`).

Stand 15.09.2026: **design-spec v1** liegt in `docs/design-spec.md`, umgesetzt auf Branch `design/v1` (Tokens in `app/globals.css`, Archivo Variable in `app/fonts/`). Grundlage war Pascals «AI Design & Build Prompt» statt einer separaten Explorationsrunde (ADR-014). Nächste Versionen weiterhin als `design-spec-vN.md` hier ablegen und nach `docs/design-spec.md` kopieren.
